language: julia
julia:
  - 1.0
  - 1.1
  - 1.4
  - nightly
matrix:
  allow_failures:
    - julia: nightly
os:
  - linux
  - osx
notifications:
  email: false
after_success:
    - julia -e 'using Pkg; pkg"add Coverage"; using Coverage; Codecov.submit(Codecov.process_folder())'
